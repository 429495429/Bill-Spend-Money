to set up virtual environmnet
python3 -m venv env

to activate virtual environment
sourct env/bin/activate

to install django
pip3 install django

to start a project
django-admin startproject <projectname>